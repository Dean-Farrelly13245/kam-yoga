# Kam Yoga Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Note: Additional environment variables are set in Supabase Edge Functions
# via `supabase secrets set` command (not in this file):
#
# - SUPABASE_SERVICE_ROLE_KEY (for Edge Functions)
# - STRIPE_SECRET_KEY (test mode: sk_test_...)
# - STRIPE_WEBHOOK_SECRET (webhook signing secret: whsec_...)
# - APP_URL (deployed site URL, e.g., https://kamyoga.netlify.app)
